package com.giovanna.amatucci.foodbook.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * @param PrimaryLight The main brand color. Used for prominent components like action buttons, active selection, progress indicators.
 */
val PrimaryLight = Color(0xFFFEAB55)

/**
 * @param OnPrimaryLight Color for text and icons displayed ON TOP OF the [PrimaryLight] color. Ensures contrast and readability.
 */
val OnPrimaryLight = Color(0xFFFFFFFF)

/**
 * @param PrimaryContainerLight A lighter tone of the primary color. Used for less prominent containers, such as tags, dialog boxes, or less prominent header backgrounds.
 */
val PrimaryContainerLight = Color(0xFFFFDDB7)

/**
 * @param OnPrimaryContainerLight Color for text and icons displayed ON TOP OF the [PrimaryContainerLight] color. Ensures contrast and readability.
 */
val OnPrimaryContainerLight = Color(0xFF2B1700)

/**
 * @param SecondaryLight A secondary brand color. Used to highlight sections, filters, or elements with less importance than the primary one, but still relevant.
 */
val SecondaryLight = Color(0xFF705C41)

/**
 * @param OnSecondaryLight Color for text and icons displayed ON TOP OF the [SecondaryLight] color. Ensures contrast and readability.
 */
val OnSecondaryLight = Color(0xFFFFFFFF)

/**
 * @param SecondaryContainerLight A lighter tone of the secondary color. Used for secondary containers, such as inactive selection chips or alternative card backgrounds.
 */
val SecondaryContainerLight = Color(0xFFFADFAC)

/**
 * @param OnSecondaryContainerLight Color for text and icons displayed ON TOP OF the [SecondaryContainerLight] color. Ensures contrast and readability.
 */
val OnSecondaryContainerLight = Color(0xFF281A05)

/**
 * @param TertiaryLight A tertiary color (accent). Used to highlight specific elements, such as subtle progress bars, highlighted text, or graphic elements.
 */
val TertiaryLight = Color(0xFF54643E)

/**
 * @param OnTertiaryLight Color for text and icons displayed ON TOP OF the [TertiaryLight] color. Ensures contrast and readability.
 */
val OnTertiaryLight = Color(0xFFFFFFFF)

/**
 * @param TertiaryContainerLight A lighter tone of the tertiary color. Used for accent containers or small highlight elements.
 */
val TertiaryContainerLight = Color(0xFFD8EABC)

/**
 * @param OnTertiaryContainerLight Color for text and icons displayed ON TOP OF the [TertiaryContainerLight] color. Ensures contrast and readability.
 */
val OnTertiaryContainerLight = Color(0xFF131F03)

/**
 * @param ErrorLight Color to indicate error states. Used for error messages, validation icons, and borders of invalid fields.
 */
val ErrorLight = Color(0xFFBA1A1A)

/**
 * @param OnErrorLight Color for text and icons displayed ON TOP OF the [ErrorLight] color. Ensures contrast and readability in error messages.
 */
val OnErrorLight = Color(0xFFFFFFFF)

/**
 * @param ErrorContainerLight A lighter tone of the error color. Used for error warning backgrounds or error message containers.
 */
val ErrorContainerLight = Color(0xFFFFDAD6)

/**
 * @param OnErrorContainerLight Color for text and icons displayed ON TOP OF the [ErrorContainerLight] color. Ensures contrast and readability in error containers.
 */
val OnErrorContainerLight = Color(0xFF410002)

/**
 * @param BackgroundLight The main background color of the screen.
 */
val BackgroundLight = Color(0xFFFFFAED)

/**
 * @param OnBackgroundLight Color for text and icons displayed ON TOP OF the [BackgroundLight] color.
 */
val OnBackgroundLight = Color(0xFF1F1B16)

/**
 * @param SurfaceLight The main color for component surfaces such as Cards, BottomSheets, Menus, and Dialogs.
 */
val SurfaceLight = Color(0xFFFBEAD5)

/**
 * @param OnSurfaceLight Color for text and icons displayed ON TOP OF the [SurfaceLight] color.
 */
val OnSurfaceLight = Color(0xFF1F1B16)

/**
 * @param SurfaceVariantLight An alternative surface color, usually darker or with a different hue. Used for search boxes, dividers, or other elements that need to stand out from the main surface.
 */
val SurfaceVariantLight = Color(0xFFF0E0CF)

/**
 * @param OnSurfaceVariantLight Color for text and icons displayed ON TOP OF the [SurfaceVariantLight] color.
 */
val OnSurfaceVariantLight = Color(0xFF4E4539)

/**
 * @param OutlineLight Color for fine borders, inactive text field outlines, or dividers.
 */
val OutlineLight = Color(0xFF807567)

/**
 * @param ScrimLight A translucent color used for overlays, such as modal backgrounds, to darken the content below.
 */
val ScrimLight = Color(0xFF000000)

/**
 * @param InverseSurfaceLight A contrasting surface color that inverts the normal Surface/OnSurface relationship. Useful for tooltips in light themes or to highlight parts of the UI.
 */
val InverseSurfaceLight = Color(0xFF35302A)

/**
 * @param InverseOnSurfaceLight Color for text and icons displayed ON TOP OF the [InverseSurfaceLight] color.
 */
val InverseOnSurfaceLight = Color(0xFFF9EFE7)

/**
 * @param InversePrimaryLight A contrasting primary color, used in conjunction with [InverseSurfaceLight] to maintain brand readability.
 */
val InversePrimaryLight = Color(0xFFFFB961)


// Palette Generated for Dark Theme - Usages are the same as the light theme, but colors are adapted for the dark context.

/**
 * @param PrimaryDark The main brand color for the dark theme. Used for prominent components like action buttons, active selection, progress indicators.
 */
val PrimaryDark = Color(0xFFFFB961)

/**
 * @param OnPrimaryDark Color for text and icons displayed ON TOP OF the [PrimaryDark] color. Ensures contrast and readability.
 */
val OnPrimaryDark = Color(0xFF472A00)

/**
 * @param PrimaryContainerDark A darker tone of the primary color. Used for less prominent containers, such as tags, dialog boxes, or less prominent header backgrounds.
 */
val PrimaryContainerDark = Color(0xFF663E00)

/**
 * @param OnPrimaryContainerDark Color for text and icons displayed ON TOP OF the [PrimaryContainerDark] color. Ensures contrast and readability.
 */
val OnPrimaryContainerDark = Color(0xFFFFDDB7)

/**
 * @param SecondaryDark A secondary brand color for the dark theme. Used to highlight sections, filters, or elements with less importance than the primary one, but still relevant.
 */
val SecondaryDark = Color(0xFFDEC392)

/**
 * @param OnSecondaryDark Color for text and icons displayed ON TOP OF the [SecondaryDark] color. Ensures contrast and readability.
 */
val OnSecondaryDark = Color(0xFF3F2E17)

/**
 * @param SecondaryContainerDark A darker tone of the secondary color. Used for secondary containers, such as inactive selection chips or alternative card backgrounds.
 */
val SecondaryContainerDark = Color(0xFF57442B)

/**
 * @param OnSecondaryContainerDark Color for text and icons displayed ON TOP OF the [SecondaryContainerDark] color. Ensures contrast and readability.
 */
val OnSecondaryContainerDark = Color(0xFFFADFAC)

/**
 * @param TertiaryDark A tertiary color (accent) for the dark theme. Used to highlight specific elements, such as subtle progress bars, highlighted text, or graphic elements.
 */
val TertiaryDark = Color(0xFFBCCEA2)

/**
 * @param OnTertiaryDark Color for text and icons displayed ON TOP OF the [TertiaryDark] color. Ensures contrast and readability.
 */
val OnTertiaryDark = Color(0xFF273514)

/**
 * @param TertiaryContainerDark A darker tone of the tertiary color. Used for accent containers or small highlight elements.
 */
val TertiaryContainerDark = Color(0xFF3D4C28)

/**
 * @param OnTertiaryContainerDark Color for text and icons displayed ON TOP OF the [TertiaryContainerDark] color. Ensures contrast and readability.
 */
val OnTertiaryContainerDark = Color(0xFFD8EABC)

/**
 * @param ErrorDark Color to indicate error states in the dark theme. Used for error messages, validation icons, and borders of invalid fields.
 */
val ErrorDark = Color(0xFFFFB4AB)

/**
 * @param OnErrorDark Color for text and icons displayed ON TOP OF the [ErrorDark] color. Ensures contrast and readability in error messages.
 */
val OnErrorDark = Color(0xFF690005)

/**
 * @param ErrorContainerDark A darker tone of the error color. Used for error warning backgrounds or error message containers.
 */
val ErrorContainerDark = Color(0xFF93000A)

/**
 * @param OnErrorContainerDark Color for text and icons displayed ON TOP OF the [ErrorContainerDark] color. Ensures contrast and readability in error containers.
 */
val OnErrorContainerDark = Color(0xFFFFDAD6)

/**
 * @param BackgroundDark The main background color of the screen for the dark theme.
 */
val BackgroundDark = Color(0xFF000000)

/**
 * @param OnBackgroundDark Color for text and icons displayed ON TOP OF the [BackgroundDark] color.
 */
val OnBackgroundDark = Color(0xFFEAE1D9)

/**
 * @param SurfaceDark The main color for component surfaces such as Cards, BottomSheets, Menus, and Dialogs in the dark theme.
 */
val SurfaceDark = Color(0xFF1F1B16)

/**
 * @param OnSurfaceDark Color for text and icons displayed ON TOP OF the [SurfaceDark] color.
 */
val OnSurfaceDark = Color(0xFFEAE1D9)

/**
 * @param SurfaceVariantDark An alternative surface color, usually lighter or with a different hue. Used for search boxes, dividers, or other elements that need to stand out from the main surface in the dark theme.
 */
val SurfaceVariantDark = Color(0xFF4E4539)

/**
 * @param OnSurfaceVariantDark Color for text and icons displayed ON TOP OF the [SurfaceVariantDark] color.
 */
val OnSurfaceVariantDark = Color(0xFFD2C4B4)

/**
 * @param OutlineDark Color for fine borders, inactive text field outlines, or dividers in the dark theme.
 */
val OutlineDark = Color(0xFF9A8F80)

/**
 * @param ScrimDark A translucent color used for overlays, such as modal backgrounds, to darken the content below in the dark theme.
 */
val ScrimDark = Color(0xFF000000)

/**
 * @param InverseSurfaceDark A contrasting surface color that inverts the normal Surface/OnSurface relationship. Useful for tooltips in dark themes or to highlight parts of the UI.
 */
val InverseSurfaceDark = Color(0xFFEAE1D9)

/**
 * @param InverseOnSurfaceDark Color for text and icons displayed ON TOP OF the [InverseSurfaceDark] color.
 */
val InverseOnSurfaceDark = Color(0xFF1F1B16)

/**
 * @param InversePrimaryDark A contrasting primary color, used in conjunction with [InverseSurfaceDark] to maintain brand readability in the dark theme.
 */
val InversePrimaryDark = Color(0xFF865300)

/**
 * @param RatingStarColor star rating color
 */
val RatingStarColor = Color(0xFFFFB300)